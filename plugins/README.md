# Instructions

Place directories containing prebuilt external Dive plugins under `plugins/external` before building Dive with `scripts/build.py`. The supported external plugins will be copied over to the build package folder.

# Currently supported external plugins
* dive_profiling_plugin